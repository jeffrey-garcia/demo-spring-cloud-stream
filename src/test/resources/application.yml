eventstore:
  timezone: Asia/Hong_Kong
  retry:
    autoStart: false # disable the auto retry when running integration test
    backoff:
      milliseconds: 1500
    message:
      expired:
        seconds: 1